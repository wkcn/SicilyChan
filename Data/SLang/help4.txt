(\
    (print "以下介绍#$_`四种宏")
    (print "#6+5等价于(+ 6 5)")
    (print "$max 5 6     等价于      (max 5 6)")
    (print  " _1 即获取名为'_'的数组下标为1的参数")
    (print "也就是函数被调用时传入的参数，_0为参数总数，_1为第一个参数")
    (print "`有`0 `1 `2等形式，即将表达式替换，是很强大的功能，具体可参照for实现")
    (print "还有一些特性没有在这列举，可以参考源代码实现及data里的代码实现")
)